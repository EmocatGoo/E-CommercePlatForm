<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.yyblcc.ecommerceplatforms.mapper.ProductLikeMapper">
    <insert id="insertIgnore" parameterType="map">
        INSERT IGNORE INTO tb_product_like (user_id, product_id)
        VALUES (#{userId}, #{productId})
    </insert>

    <delete id="deleteByUserIdAndProductId">
        DELETE FROM tb_product_like WHERE user_id = #{userId} AND product_id = #{productId}
    </delete>
</mapper>